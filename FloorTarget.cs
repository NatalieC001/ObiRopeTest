using UnityEngine;

/// <summary>
/// Attach this script to any floor or wall object.
/// Allows sticking arrows to register this object as a valid environment surface.
/// When hit, the arrow will stick, turn off its line renderer, and dissolve.
/// </summary>
public class FloorTarget : MonoBehaviour
{
    // Simply acts as an identifier for StickingArrow.cs
}
